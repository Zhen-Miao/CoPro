% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_constructors.R
\name{CreateCoPro}
\alias{CreateCoPro}
\title{Create a CoPro object, automatically choosing Single vs Multi and splitting large slices.}
\usage{
CreateCoPro(
  normalizedData,
  locationData,
  metaData,
  cellTypes,
  slideID = NULL,
  maxCell = 50000L,
  plot = FALSE,
  seed = 1L
)
}
\arguments{
\item{normalizedData}{matrix (cells x genes)}

\item{locationData}{data.frame (cells x coords; needs x,y, optionally z)}

\item{metaData}{data.frame (cells x annotations)}

\item{cellTypes}{vector length nrow(normalizedData)}

\item{slideID}{optional vector length nrow(normalizedData); if NULL, treated as one slice}

\item{maxCell}{integer max cells per (sub)slice (default 50000)}

\item{plot}{logical; if TRUE, plot the auto-slicing result (default FALSE)}

\item{seed}{integer seed for reproducible splitting (default 1)}
}
\description{
Behavior:
\enumerate{
\item one slice and ncell < p  -> newCoProSingle()
\item one slice and ncell > p  -> newCoProMulti() with artificial blocks
\item multi slices, all < p    -> newCoProMulti() with original slideID
\item multi slices, some > p   -> newCoProMulti() with refined slideID (origin + block)
}
}

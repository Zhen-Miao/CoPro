# Assign distance matrix manually

Assign distance matrix manually

## Usage

``` r
assignDistanceManually(object, distanceList)

# S4 method for class 'CoPro'
assignDistanceManually(object, distanceList)
```

## Arguments

- object:

  A `CoPro` object

- distanceList:

  A list object that contains all pairwise distances between any two
  pairs of cells.

## Value

A `CoPro` object with specified

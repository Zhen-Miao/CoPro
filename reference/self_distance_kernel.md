# Compute Self-Distance and Self-Kernel for Multiple Cell Types

These functions extend the CoPro package to compute within-cell-type
(self) distance and kernel matrices even when multiple cell types are
present. This is useful for analyses that need both cross-type and
within-type spatial relationships.

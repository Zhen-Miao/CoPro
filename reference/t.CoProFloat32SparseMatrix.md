# Transpose an encoded CoPro float32 sparse matrix without copying its values

Transpose an encoded CoPro float32 sparse matrix without copying its
values

## Usage

``` r
# S3 method for class 'CoProFloat32SparseMatrix'
t(x)
```

## Arguments

- x:

  A `CoProFloat32SparseMatrix`.

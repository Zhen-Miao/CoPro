% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12c_float32_sparse_kernel.R
\name{materializeFloat32Kernels}
\alias{materializeFloat32Kernels}
\title{Materialize all encoded kernels in a CoPro object}
\usage{
materializeFloat32Kernels(object, verbose = TRUE)
}
\arguments{
\item{object}{A \code{CoPro} object.}

\item{verbose}{Whether to report how many kernels were converted.}
}
\value{
A copy of \code{object} whose kernel list contains standard sparse
matrices.
}
\description{
Converts every \code{CoProFloat32SparseMatrix} in \code{object@kernelMatrices} to a
standard double-precision sparse \code{Matrix}. This is a compatibility escape
hatch for third-party or older code that accesses the slot directly rather
than using \code{\link[=getKernelMatrix]{getKernelMatrix()}}. It can temporarily require both encoded and
decoded copies, so it should be avoided in memory-critical analysis steps.
}

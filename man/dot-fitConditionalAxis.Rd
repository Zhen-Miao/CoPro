% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/D_spatial_CCA_permutation.R
\name{.fitConditionalAxis}
\alias{.fitConditionalAxis}
\title{Fit a single conditional canonical axis (internal kernel)}
\usage{
.fitConditionalAxis(
  PCmats,
  flat_kernels,
  sigma,
  cts,
  W_lower = NULL,
  k_minus_1 = 0,
  Y_resi = NULL,
  kernel_info = NULL,
  sdev2_list = NULL,
  maxIter = 200,
  tol = 1e-05
)
}
\arguments{
\item{PCmats}{Named list of (possibly permuted) PC matrices, one per cell type.}

\item{flat_kernels}{Flat kernel list from the CoPro object.}

\item{sigma}{Kernel bandwidth (numeric).}

\item{cts}{Cell types of interest.}

\item{W_lower}{Named list of observed weight matrices whose first
\code{k_minus_1} columns are the fixed deflation directions; ignored when
\code{k_minus_1 = 0}.}

\item{k_minus_1}{Number of lower axes to deflate (0 for the first axis).}

\item{Y_resi}{Optional precomputed \code{compute_Y_resi()} structure for this
(PCmats, sigma); recomputed when \code{NULL} and \code{k_minus_1 >= 1}.}

\item{kernel_info}{Optional precomputed kernel and normalizer information
from \code{.get_ncorr_kernel_info()}.}

\item{sdev2_list}{Optional diagonal CCA metric used when \code{scalePCs = FALSE}.}

\item{maxIter, tol}{Optimization controls.}
}
\value{
List with \code{w} (named list of 1-column weight matrices for axis k) and
\code{ncorr} (its normalized correlation).
}
\description{
Internal numeric kernel for the sequential step-down permutation test. Given
(possibly permuted) PC matrices and the FIXED observed lower-axis weight
directions, it returns the leading canonical axis of the residual after those
lower directions have been deflated, together with that axis' normalized
correlation.
}
\details{
For axis \code{k = 1} (no lower directions; \code{k_minus_1 = 0}) this is exactly the
first-component optimization used by \code{\link[=runSkrCCAPermu_FairSigma]{runSkrCCAPermu_FairSigma()}}, so the
\code{k = 1} conditional test reproduces the fair-sigma CC1 test bit-for-bit.

For \code{k >= 2} it deflates the observed CC1..CC(k-1) directions from the
cross-product \verb{Y = t(X_i) K_ij X_j} in feature (PC) space, using the SAME
fixed observed directions on every permutation, then optimizes the leading
residual component. Deflating each permutation by the same observed
directions (rather than by that permutation's own leading axis) is what makes
the higher-axis null exchangeable with the observed statistic and removes the
anti-conservative bias of the naive per-axis permutation p-value.

Under whitened PCs (\code{scalePCs = TRUE}) the fixed directions are removed with
the full projection \verb{(I - u u^T) Y (I - v v^T)}. The simpler rank-one
subtraction is not used here: it agrees with the projection only when the
fixed directions are singular vectors of the current \code{Y}, which is generally
false after permutation. For \code{scalePCs = FALSE}, the corresponding weighted
oblique projection is used.

The expensive \code{compute_Y_resi()} can be computed once per (permutation,
sigma) and reused across axes by passing it as \code{Y_resi}; deflation does not
mutate the supplied structure (copy-on-modify), so the same \code{Y_resi} is safe
to reuse for every \code{k}.
}
\keyword{internal}

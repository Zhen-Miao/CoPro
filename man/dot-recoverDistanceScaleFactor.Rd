% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/C_resampling_function.R
\name{.recoverDistanceScaleFactor}
\alias{.recoverDistanceScaleFactor}
\title{Recover the raw-to-normalized distance scaling factor}
\usage{
.recoverDistanceScaleFactor(object, n_probe = 200L)
}
\arguments{
\item{object}{A CoPro object with \verb{@distances} populated.}

\item{n_probe}{Number of random cell pairs to probe (default 200).}
}
\value{
Numeric scaling factor (normalized = raw * factor), or \code{NA_real_}
if it cannot be recovered.
}
\description{
Internal helper that recovers the single scalar mapping raw
spatial-coordinate distances to the normalized distance scale on which
\code{sigmaValues} are defined. \code{computeDistance(normalizeDistance = TRUE)}
multiplies every stored distance by one constant, so for any entry that was
not affected by low-distance truncation the ratio (normalized distance) /
(raw Euclidean distance) equals that constant. We probe random off-diagonal
entries and take the median ratio, which is robust to the small fraction of
truncated entries and avoids copying the full distance matrix.
}
\details{
This assumes isotropic coordinates (\code{xDistScale = yDistScale}, the default).
Under anisotropic scaling the recovered value is a representative, not exact,
factor; the \verb{[sigma, 4*sigma]} guardrail in \code{\link[=.sigmaAwareBins]{.sigmaAwareBins()}} will flag a
grossly mis-sized grid. When \code{normalizeDistance = FALSE} the recovered factor
is simply the coordinate scale (1 by default), which is also correct because
\code{sigma} is then on the raw coordinate scale.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/H_extrapolate.R
\name{transfer_scores}
\alias{transfer_scores}
\title{Transfer cell scores between matrices using gene weights}
\usage{
transfer_scores(
  mat_A,
  mat_B,
  gs_ct,
  use_quantile_normalization = TRUE,
  gs_weight_threshold = 0.05,
  verbose = TRUE
)
}
\arguments{
\item{mat_A}{Numeric matrix of shape cells-by-genes (reference). Column order
must correspond to genes in \code{gs_ct}.}

\item{mat_B}{Numeric matrix of shape cells-by-genes (target). Must have the
same genes (columns) and order as \code{mat_A}.}

\item{gs_ct}{Numeric matrix of gene weights of shape genes-by-K, where K is
the number of signatures or cell types. Column names, if present, are used
in verbose messages.}

\item{use_quantile_normalization}{Logical; whether to quantile-normalize
\code{mat_B} to \code{mat_A} per feature before standardization (default \code{TRUE}).}

\item{gs_weight_threshold}{Numeric; absolute threshold used to zero out small
gene weights in \code{gs_ct} (default \code{0.05}).}

\item{verbose}{Logical; whether to print progress messages (default \code{TRUE}).}
}
\value{
A numeric matrix of cell scores with shape (nrow(\code{mat_B}) x ncol(\code{gs_ct})).
}
\description{
Given reference (\code{mat_A}) and target (\code{mat_B}) cell-by-gene matrices with the
same set and order of genes (columns), this function optionally quantile
normalizes the target to match the reference per feature, standardizes the
target using the reference mean and standard deviation, filters small-magnitude
gene weights, and computes cell scores via matrix multiplication
\eqn{B_{standardized} \\times W}.
}
\details{
\itemize{
\item If \code{use_quantile_normalization = TRUE}, \code{mat_B} is quantile-normalized to
the distribution of \code{mat_A} for each gene.
\item Columns of the normalized \code{mat_B} are then centered and scaled using the
column means and standard deviations computed from \code{mat_A} (with a small
safeguard for near-zero standard deviations).
\item Gene weights in \code{gs_ct} are thresholded by absolute value per column; values
with \code{abs(weight) < gs_weight_threshold} are set to 0.
\item Final cell scores are obtained as the matrix product of the standardized
\code{mat_B} with the filtered weight matrix \code{gs_ct}.
}

This function assumes that \code{mat_A}, \code{mat_B}, and \code{gs_ct} are aligned
on the same set and ordering of genes (columns of \code{mat_A}/\code{mat_B}, rows of
\code{gs_ct}). Callers are responsible for subsetting/reordering if needed.
}
\seealso{
\link{getTransferCellScores}
}
\keyword{internal}

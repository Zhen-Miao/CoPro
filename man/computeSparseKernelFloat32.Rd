% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12c_float32_sparse_kernel.R
\name{computeSparseKernelFloat32}
\alias{computeSparseKernelFloat32}
\alias{computeSparseKernelFloat32,CoProSingle-method}
\alias{computeSparseKernelFloat32,CoProMulti-method}
\title{Compute block-streamed float32 sparse Gaussian kernels}
\usage{
computeSparseKernelFloat32(
  object,
  sigmaValues,
  lowerLimit = 1e-07,
  upperQuantile = 0.85,
  normalizeKernel = FALSE,
  minAveCellNeighor = 2,
  rowNormalizeKernel = FALSE,
  colNormalizeKernel = FALSE,
  distType = c("Euclidean2D", "Euclidean3D"),
  xDistScale = 1,
  yDistScale = 1,
  zDistScale = 1,
  normalizeDistance = TRUE,
  normalizeTarget = 0.01,
  truncateLowDist = TRUE,
  overwrite = TRUE,
  verbose = TRUE
)

\S4method{computeSparseKernelFloat32}{CoProSingle}(
  object,
  sigmaValues,
  lowerLimit = 1e-07,
  upperQuantile = 0.85,
  normalizeKernel = FALSE,
  minAveCellNeighor = 2,
  rowNormalizeKernel = FALSE,
  colNormalizeKernel = FALSE,
  distType = c("Euclidean2D", "Euclidean3D"),
  xDistScale = 1,
  yDistScale = 1,
  zDistScale = 1,
  normalizeDistance = TRUE,
  normalizeTarget = 0.01,
  truncateLowDist = TRUE,
  overwrite = TRUE,
  verbose = TRUE
)

\S4method{computeSparseKernelFloat32}{CoProMulti}(
  object,
  sigmaValues,
  lowerLimit = 1e-07,
  upperQuantile = 0.85,
  normalizeKernel = FALSE,
  minAveCellNeighor = 2,
  rowNormalizeKernel = FALSE,
  colNormalizeKernel = FALSE,
  distType = c("Euclidean2D", "Euclidean3D"),
  xDistScale = 1,
  yDistScale = 1,
  zDistScale = 1,
  normalizeDistance = TRUE,
  normalizeTarget = 0.01,
  truncateLowDist = TRUE,
  overwrite = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{CoProSingle} or \code{CoProMulti} object.}

\item{sigmaValues}{Positive Gaussian-kernel scale values.}

\item{lowerLimit}{Kernel support threshold.}

\item{upperQuantile}{Quantile at which large kernel values are clipped.}

\item{normalizeKernel}{Whether to divide all entries by the median positive
row sum.}

\item{minAveCellNeighor}{Minimum average represented neighbors.}

\item{rowNormalizeKernel, colNormalizeKernel}{Whether to normalize each
nonempty row or column to sum to one. They cannot both be \code{TRUE}.}

\item{distType}{\code{"Euclidean2D"} or \code{"Euclidean3D"}.}

\item{xDistScale, yDistScale, zDistScale}{Per-axis coordinate scales.}

\item{normalizeDistance}{Whether to normalize the low distance percentile.}

\item{normalizeTarget}{Target low distance percentile after normalization.}

\item{truncateLowDist}{Whether to floor very small distances.}

\item{overwrite}{Whether to replace existing kernel matrices.}

\item{verbose}{Whether to report progress.}
}
\value{
The object with encoded float32 kernels in \verb{@kernelMatrices}.
}
\description{
A large-data kernel path that never materializes a float64 sparse kernel.
Each cell-type block is enumerated once at the largest requested radius,
distances are retained temporarily as float32, and every sigma-specific
kernel is written directly as a row-compressed float32 byte buffer. The
block's neighbor data are released before the next block.
}
\details{
The resulting kernels are consumed directly by \code{\link[=runSkrCCA]{runSkrCCA()}} through a
parallel float32 \verb{X1' K X2} operator. Only the small PC-space result is
converted to float64. Unnormalized and globally normalized one-cell-type
kernels store only their strict upper triangle and are applied as symmetric
matrices without expansion. Row- or column-normalized self-kernels are
expanded because those operations make the represented matrix asymmetric.

Both single- and multi-slide objects are supported, with any positive number
of cell types. Multi-slide construction streams one slide/cell-type block at
a time and schedules the largest block first. Older consumers can request a
temporary standard sparse matrix through \code{\link[=getKernelMatrix]{getKernelMatrix()}} or
\code{\link[=asDoubleSparseMatrix]{asDoubleSparseMatrix()}}.

Global, row, and column normalization are applied directly to float32 values.
The ordinary centered Frobenius objective normalizer is also evaluated from
the encoded float32 kernel without materialization. Exact whitened Frobenius
normalization currently uses the compatibility path through temporary
double-precision sparse matrices when within-type operators are supplied.
}
\seealso{
\code{\link[=computeSparseKernel]{computeSparseKernel()}}, \code{\link[=runSkrCCA]{runSkrCCA()}}, \code{\link[=asDoubleSparseMatrix]{asDoubleSparseMatrix()}}

Other spatial-pipeline:
\code{\link{computeDistance}()},
\code{\link{computeKernelMatrix}()},
\code{\link{computePCA}()},
\code{\link{computeSparseKernel}()},
\code{\link{runGeneSpaceCCA}()},
\code{\link{runSkrCCA}()},
\code{\link{runSlideLevelInference}()}
}
\concept{spatial-pipeline}

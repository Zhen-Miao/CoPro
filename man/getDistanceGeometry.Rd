% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11c_distance_geometry.R
\name{getDistanceGeometry}
\alias{getDistanceGeometry}
\alias{getDistanceGeometry,CoPro-method}
\title{Get the coordinate geometry a CoPro object's distances and kernels use}
\usage{
getDistanceGeometry(object)

\S4method{getDistanceGeometry}{CoPro}(object)
}
\arguments{
\item{object}{A \code{CoPro} object.}
}
\value{
A named list with \code{distType}, \code{xDistScale}, \code{yDistScale},
\code{zDistScale}, \code{normalizeDistance}, \code{normalizeTarget}, \code{truncateLowDist},
and \code{source} (the function that wrote the record). \code{NULL} when no
distance- or kernel-building step has run, or when \code{object} predates the
\code{distanceGeometry} slot.
}
\description{
Returns the record written by whichever step built the object's coordinate
basis -- \code{\link[=computeDistance]{computeDistance()}}, \code{\link[=computeSelfDistance]{computeSelfDistance()}},
\code{\link[=computeSparseKernel]{computeSparseKernel()}}, or \code{\link[=computeKernelMatrix]{computeKernelMatrix()}} on its sparse path. Use
it whenever an analysis has to reproduce the geometry the kernels were fit
on, for instance to rebuild a spatial neighbor graph for a permutation null.
}
\details{
The record persists after \code{computeKernelMatrix(dropDistances = TRUE)} (the
default) has cleared \verb{@distances}, so a stored object still knows which
coordinates its kernels live on.
}
\examples{
toy <- readRDS(system.file("extdata", "toy_copro_data.rds", package = "CoPro"))
obj <- newCoProSingle(
  normalizedData = toy$normalizedData,
  locationData   = toy$locationData,
  metaData       = toy$metaData,
  cellTypes      = toy$cellTypes
)
obj <- subsetData(obj, cellTypesOfInterest = unique(toy$cellTypes))
obj <- computeDistance(obj, distType = "Euclidean2D", verbose = FALSE)
getDistanceGeometry(obj)
}
\seealso{
\code{\link[=computeDistance]{computeDistance()}}, \code{\link[=computeKernelMatrix]{computeKernelMatrix()}}

Other accessors: 
\code{\link{getCellScores}()},
\code{\link{getCellScoresInSitu}()},
\code{\link{getDistMat}()},
\code{\link{getKernelMatrix}()}
}
\concept{accessors}

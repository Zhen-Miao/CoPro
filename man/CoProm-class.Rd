% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/K_multi-slide.R
\docType{class}
\name{CoProm-class}
\alias{CoProm-class}
\title{CoProm object for Multi-Slide Spatial Transcriptomics Analysis}
\description{
An S4 class to store and manage data for multi-slide spatial transcriptomics
analysis using the CoPro methodology.
}
\section{Slots}{

\describe{
\item{\code{slideID}}{A \code{factorOrCharacter} vector indicating the slide/sample origin for each cell.}

\item{\code{slideList}}{A \code{character} vector storing the unique slide identifiers present.}

\item{\code{normalizedData}}{A \code{matrixOrSparseMatrix} object storing the combined normalized data for all cells across slides.}

\item{\code{locationData}}{A \code{matrixOrDataFrame} object storing the combined location data.}

\item{\code{metaData}}{A \code{data.frame} object storing the combined metadata. Must include columns matching cell barcodes in rows.}

\item{\code{cellTypes}}{A \code{factorOrCharacter} vector storing cell type labels for all cells.}

\item{\code{geneList}}{A \code{character} vector storing gene names (colnames of \code{normalizedData}).}

\item{\code{cellTypesOfInterest}}{A \code{character} vector specifying the cell types to include in the analysis.}

\item{\code{normalizedDataSub}}{A \code{matrixOrSparseMatrix} storing the subset of \code{normalizedData} for cells matching \code{cellTypesOfInterest}.}

\item{\code{locationDataSub}}{A \code{matrixOrDataFrame} storing the subset of \code{locationData}.}

\item{\code{metaDataSub}}{A \code{data.frame} storing the subset of \code{metaData}, including the \code{slideID} column.}

\item{\code{cellTypesSub}}{A \code{factorOrCharacter} storing the subset of \code{cellTypes}.}

\item{\code{integratedData}}{A \code{list} object to store the output from data integration (e.g., Seurat integrated assay or corrected values), typically structured by cell type. Format depends on integration method output.}

\item{\code{pcaResults}}{A \code{list} object storing PCA results after integration.
Recommended structure: \code{list(slideID = list(cellType = pc_matrix))}.}

\item{\code{pcaGlobal}}{A \code{list} object storing PCA results for each cell type.}

\item{\code{nPCA}}{A \code{numeric} value specifying the number of PCs used.}

\item{\code{scalePCs}}{A \code{logical} value indicating if PCs were scaled before skrCCA (usually FALSE if PCA done on integrated data).}

\item{\code{distances}}{A \code{list} object storing pairwise distances, structured by slide: \code{list(slideID = list(cellType1 = list(cellType2 = dist_matrix)))}.}

\item{\code{kernelMatrices}}{A \code{list} object storing kernel matrices: \code{list(sigma = list(slideID = list(cellType1 = list(cellType2 = kernel_matrix))))}.}

\item{\code{sigmaValues}}{A \code{numeric} vector storing the sigma values used for kernel generation.}

\item{\code{sigmaValueChoice}}{A \code{numeric} value storing the chosen sigma based on correlation analysis.}

\item{\code{nCC}}{A \code{numeric} value specifying the number of canonical components computed.}

\item{\code{skrCCAOut}}{A \code{list} object storing the shared weight vectors from multi-slide skrCCA: \code{list(sigma = list(cellType = weight_matrix))}.}

\item{\code{normalizedCorrelation}}{A \code{list} object storing normalized correlation results. Structure depends on calculation (e.g., \code{list(sigma = list(slideID = correlation_df))} or \code{list(sigma = aggregate_correlation_df)}).}

\item{\code{cellScores}}{A \code{list} object storing slide-specific cell scores: \code{list(sigma = list(slideID = list(cellType = cell_score_matrix)))}.}

\item{\code{geneScores}}{A \code{list} object storing gene scores (potentially shared): \code{list(sigma = list(cellType = gene_score_matrix))}.}

\item{\code{nPermu}}{A \code{numeric} value specifying the number of permutations (if performed).}

\item{\code{skrCCAPermuOut}}{A \code{list} storing permutation results for weights.}

\item{\code{normalizedCorrelationPermu}}{A \code{list} storing permutation results for correlation.}
}}


% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/D_spatial_CCA_permutation.R
\name{calculate_pvalue}
\alias{calculate_pvalue}
\title{Calculate P-value from Permutation Results}
\usage{
calculate_pvalue(object, cc_index = 1, alternative = "greater")
}
\arguments{
\item{object}{A CoPro object with permutation results}

\item{cc_index}{Which canonical correlation component to use (default: 1)}

\item{alternative}{Direction of test: "greater" (default), "less", or "two.sided"}
}
\value{
List with the Phipson & Smyth (2010) permutation p-value (\code{p_value},
never exactly zero), the Monte-Carlo floor \code{mc_floor = 1 / (n_permu + 1)},
the observed value, and the permutation distribution. With more than one
cell-type pair, the observed and each permutation statistic are both the
maximum normalized correlation over pairs for the requested axis.
}
\description{
Helper function to calculate p-value from permutation testing results.
}
\examples{
\dontrun{
result <- calculate_pvalue(br, cc_index = 1, alternative = "greater")
print(paste("P-value:", result$p_value))
}

}
\references{
Phipson B, Smyth GK (2010). Permutation P-values should never be
zero. \emph{Stat Appl Genet Mol Biol} 9:Article39.
}

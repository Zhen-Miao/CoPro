% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04b_optimization_sumcor.R
\name{optimize_sumcor_pca}
\alias{optimize_sumcor_pca}
\title{PCA-space SUMCOR optimization -- first component}
\usage{
optimize_sumcor_pca(
  X_list_all,
  flat_kernels,
  sigma,
  slides,
  cell_types,
  slideWeight = "equal",
  sdev2_list = NULL,
  max_iter = 200,
  tol = 1e-06,
  n_cores = 1,
  verbose = FALSE,
  ops = NULL
)
}
\arguments{
\item{X_list_all}{\code{X_list_all[[slide]][[cellType]]} cell-by-PC matrices.}

\item{flat_kernels}{Flat list of kernel matrices.}

\item{sigma}{Kernel bandwidth (numeric scalar).}

\item{slides}{Slide IDs.}

\item{cell_types}{Cell types to optimize over.}

\item{slideWeight}{Per-slide weighting: \code{"equal"} (default) or \code{"size"}.}

\item{sdev2_list}{Optional named list of squared standard deviations per cell
type, supplied when \code{scalePCs = FALSE}.}

\item{max_iter}{Maximum projected-gradient iterations.}

\item{tol}{Convergence tolerance on the projected-gradient norm.}

\item{n_cores}{Cores for the per-slide kernel products.}

\item{verbose}{Report progress.}

\item{ops}{Optional precomputed \code{.computeSlideOperators()} structure.}
}
\value{
Named list of single-column weight matrices, with attributes
\code{"objective"} and \code{"slideWeight"}.
}
\description{
Maximizes the per-slide self-normalized objective
\deqn{f(w) = \frac{\sum_s \sum_{i<j} m_{ij}^{(s)}\, w_i' Y_{ij}^{(s)} w_j /
  (\sigma_i^{(s)} \sigma_j^{(s)})}{\sum_s \sum_{i<j} m_{ij}^{(s)}}}
where \eqn{\sigma_i^{(s)} = \sqrt{w_i' X_i^{(s)'} X_i^{(s)} w_i}} and
\eqn{m_{ij}^{(s)}} is 1 for \code{slideWeight = "equal"} or
\eqn{\sqrt{n_i^{(s)} n_j^{(s)}}} for \code{"size"}.
}
\details{
When one slide has coinciding per-pair constants (see the file header), the
existing SUMCOV result is used directly and no SUMCOR iteration is run.
}
\keyword{internal}

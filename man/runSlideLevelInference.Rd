% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/D_spatial_CCA_permutation.R
\name{runSlideLevelInference}
\alias{runSlideLevelInference}
\title{Replicate-level inference for multi-slide CoPro analyses}
\usage{
runSlideLevelInference(
  object,
  cc_index = 1,
  sigma_values = NULL,
  replicate_id = NULL,
  alternative = "greater",
  n_resamples = 9999,
  conf_level = 0.95,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{CoProMulti} object after \code{\link[=computePCA]{computePCA()}} and kernel
construction. Exactly two cell types are currently supported.}

\item{cc_index}{Canonical axis to evaluate.}

\item{sigma_values}{Candidate sigma values. Defaults to all fitted kernel
bandwidths.}

\item{replicate_id}{Optional slide-to-replicate mapping. Supply a vector
named by \code{getSlideList(object)}; an unnamed vector is matched in slide
order. Defaults to one independent replicate per slide.}

\item{alternative}{One of \code{"greater"}, \code{"less"}, or \code{"two.sided"}.}

\item{n_resamples}{Number of Monte-Carlo sign flips and bootstrap resamples.}

\item{conf_level}{Bootstrap confidence level.}

\item{seed}{Optional random seed.}

\item{verbose}{Print fold progress.}
}
\value{
A list of class \code{CoProSlideInference} containing the equal-replicate
effect, confidence interval, replicate sign-flip p-value, Monte-Carlo
floor, and one held-out result per replicate.
}
\description{
Estimates an equal-replicate spatial coordination effect without treating
cells as biological replicates. For each replicate, canonical weights and
the bandwidth are learned from all other replicates and evaluated only on
the held-out replicate. The held-out effects are combined with equal weight.
}
\details{
\code{replicate_id} may group several slides from the same donor. Every fold then
leaves out all slides belonging to one donor. Sigma selection is performed
using training slides only, so the held-out effect is not evaluated at a
bandwidth selected from that replicate.

The p-value uses sign flips of the replicate-level held-out effects and
therefore assumes independent replicates and a symmetric null distribution
around zero. Up to 15 replicates all sign patterns are enumerated exactly;
larger analyses use \code{n_resamples} Monte-Carlo sign flips with the
Phipson--Smyth correction. The confidence interval is a percentile bootstrap
over replicates. Both summaries target the equal-replicate mean.
}
\seealso{
Other spatial-pipeline:
\code{\link{computeDistance}()},
\code{\link{computeKernelMatrix}()},
\code{\link{computePCA}()},
\code{\link{computeSparseKernel}()},
\code{\link{computeSparseKernelFloat32}()},
\code{\link{runGeneSpaceCCA}()},
\code{\link{runSkrCCA}()}
}
\concept{spatial-pipeline}

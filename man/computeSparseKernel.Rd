% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12b_sparse_kernel.R
\name{computeSparseKernel}
\alias{computeSparseKernel}
\alias{computeSparseKernel,CoProSingle-method}
\alias{computeSparseKernel,CoProMulti-method}
\title{Compute sparse Gaussian kernels directly from coordinates}
\usage{
computeSparseKernel(
  object,
  sigmaValues,
  lowerLimit = 1e-07,
  upperQuantile = 0.85,
  normalizeKernel = FALSE,
  minAveCellNeighor = 2,
  rowNormalizeKernel = FALSE,
  colNormalizeKernel = FALSE,
  distType = c("Euclidean2D", "Euclidean3D"),
  xDistScale = 1,
  yDistScale = 1,
  zDistScale = 1,
  normalizeDistance = TRUE,
  normalizeTarget = 0.01,
  truncateLowDist = TRUE,
  verbose = TRUE
)

\S4method{computeSparseKernel}{CoProSingle}(
  object,
  sigmaValues,
  lowerLimit = 1e-07,
  upperQuantile = 0.85,
  normalizeKernel = FALSE,
  minAveCellNeighor = 2,
  rowNormalizeKernel = FALSE,
  colNormalizeKernel = FALSE,
  distType = c("Euclidean2D", "Euclidean3D"),
  xDistScale = 1,
  yDistScale = 1,
  zDistScale = 1,
  normalizeDistance = TRUE,
  normalizeTarget = 0.01,
  truncateLowDist = TRUE,
  verbose = TRUE
)

\S4method{computeSparseKernel}{CoProMulti}(
  object,
  sigmaValues,
  lowerLimit = 1e-07,
  upperQuantile = 0.85,
  normalizeKernel = FALSE,
  minAveCellNeighor = 2,
  rowNormalizeKernel = FALSE,
  colNormalizeKernel = FALSE,
  distType = c("Euclidean2D", "Euclidean3D"),
  xDistScale = 1,
  yDistScale = 1,
  zDistScale = 1,
  normalizeDistance = TRUE,
  normalizeTarget = 0.01,
  truncateLowDist = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{CoPro} object.}

\item{sigmaValues}{A vector of sigma values used for kernel calculation.}

\item{lowerLimit}{The lower limit for the kernel function, default is 1e-7.}

\item{upperQuantile}{The quantile used for clipping the kernel values,
default is 0.85.}

\item{normalizeKernel}{Whether to normalize the kernel matrix?
Default = FALSE. Note that normalization will not affect any downstream
analyses, it is for numerical stability and easier interpretation only.}

\item{minAveCellNeighor}{What is the minimum average number of cell in the
neighbor? This step is to help set up the expected sparsity of the
kernel matrix. If a kernel sigma value is too small, this result in too
few neighbors for most cells, resulting in an overly-sparse matrix that
makes the parameter estimation hard. Thus, the sigma values that results in
an overly-sparse matrix will be removed for later analysis.}

\item{rowNormalizeKernel}{Whether the kernel matrix will be row-wise
normalized? Note that row or column wise normalization will result in an
asymmetric result in skrCCA inference.}

\item{colNormalizeKernel}{Whether the kernel matrix will be column-wise
normalized? Note that row or column wise normalization will result in an
asymmetric result in skrCCA inference.}

\item{distType}{"Euclidean2D" or "Euclidean3D" (Morphology-Aware is not
supported by the sparse path).}

\item{xDistScale, yDistScale, zDistScale}{per-axis coordinate scales.}

\item{normalizeDistance, normalizeTarget, truncateLowDist}{distance-processing
options, matching \code{\link[=computeDistance]{computeDistance()}}.}

\item{verbose}{Whether to output the progress and related information}
}
\value{
The \code{CoPro} object with sparse kernel matrices in \verb{@kernelMatrices}.
}
\description{
A fused, memory-efficient alternative to \code{\link[=computeDistance]{computeDistance()}} +
\code{\link[=computeKernelMatrix]{computeKernelMatrix()}} for large datasets. It builds, for every cell-type
pair (and within-type), a sparse Gaussian kernel using a fixed-radius
neighbor search, never forming a dense \verb{n x n} matrix. Within-type kernels
are stored with one triangle as symmetric \code{dsCMatrix} objects; cross-type
kernels, and kernels made asymmetric by row/column normalization, use
\code{dgCMatrix}. Results are numerically equivalent to the dense path (every pair
beyond the kernel's support radius is zero anyway). Distances are not stored.
}
\seealso{
\code{\link[=computeKernelMatrix]{computeKernelMatrix()}}, \code{\link[=computeDistance]{computeDistance()}}

Other spatial-pipeline: 
\code{\link{computeDistance}()},
\code{\link{computeKernelMatrix}()},
\code{\link{computePCA}()},
\code{\link{computeSparseKernelFloat32}()},
\code{\link{runGeneSpaceCCA}()},
\code{\link{runSkrCCA}()},
\code{\link{runSlideLevelInference}()}
}
\concept{spatial-pipeline}

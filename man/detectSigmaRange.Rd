% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11d_detect_sigma.R
\name{detectSigmaRange}
\alias{detectSigmaRange}
\alias{detectSigmaRange,CoPro-method}
\title{Detect a usable sigma range from the data}
\usage{
detectSigmaRange(
  object,
  minNeighbors = 5,
  maxNeighbors = 20,
  nSigma = 5L,
  nAnchor = 500L,
  nNeighbor = 128L,
  distType = NULL,
  xDistScale = NULL,
  yDistScale = NULL,
  zDistScale = NULL,
  verbose = TRUE
)

\S4method{detectSigmaRange}{CoPro}(
  object,
  minNeighbors = 5,
  maxNeighbors = 20,
  nSigma = 5L,
  nAnchor = 500L,
  nNeighbor = 128L,
  distType = NULL,
  xDistScale = NULL,
  yDistScale = NULL,
  zDistScale = NULL,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{CoProSingle} or \code{CoProMulti} object, after \code{\link[=subsetData]{subsetData()}}.}

\item{minNeighbors, maxNeighbors}{Target effective neighbor counts bracketing
the useful range. Defaults of 5 and 20 keep the kernel above the point
where cells are effectively isolated and below the point where every pair
is coupled.}

\item{nSigma}{Number of bandwidths in the recommended grid, spaced
logarithmically across the detected range.}

\item{nAnchor}{Anchor cells sampled per block.}

\item{nNeighbor}{Nearest partners retained per anchor. Truncation error in
the effective count is about \code{exp(-nNeighbor / m)}, so the default of 128
comfortably covers targets up to a few dozen neighbors.}

\item{distType, xDistScale, yDistScale, zDistScale}{Coordinate geometry. When
omitted, any geometry already recorded on the object is used.
Anchors are chosen by a deterministic stride, not at random, so repeated
calls on the same object return the same bandwidths and the dense and sparse
kernel paths agree exactly on the distance scale.}

\item{verbose}{Whether to report per-block progress.}
}
\value{
An object of class \code{CoProSigmaRange}: a list with \code{sigmaValues} (the
recommended grid), \code{sigmaRange} (lower and upper bound), \code{feasible}
(whether one range satisfies every block), and \code{blocks} (a per-block
diagnostic \code{data.frame} with the bandwidths that block would need, its
median nearest-partner spacing, and its effective neighbor count at the
recommended bounds).
}
\description{
Chooses Gaussian bandwidths by the only quantity that is comparable across
datasets: how many neighbors a cell is effectively coupled to. For a cell
\code{a}, the effective neighbor count at bandwidth sigma is the kernel row sum
\verb{m_a(sigma) = sum_b exp(-0.5 (d_ab / sigma)^2)}. This function finds the
sigma values at which the median cell reaches \code{minNeighbors} and
\code{maxNeighbors}, for every cell-type pair (and slide), and reports a shared
range plus a recommended grid.
}
\details{
The returned bandwidths are in the coordinate units of \code{locationData}, so
they can be passed straight to \code{\link[=computeKernelMatrix]{computeKernelMatrix()}} with
\code{normalizeDistance = FALSE}. There is no need to rescale distances first:
selecting sigma from the data is what makes the analysis unit-independent.

Speed comes from sampling. Only \code{nAnchor} cells per block are examined, each
against its \code{nNeighbor} nearest partners found by fixed-radius search, so
cost scales with the sample rather than with the number of pairs. The
estimate is deliberately approximate; the downstream bandwidth selection in
\code{\link[=computeNormalizedCorrelation]{computeNormalizedCorrelation()}} refines the choice within the range.
}
\examples{
toy <- readRDS(system.file("extdata", "toy_copro_data.rds", package = "CoPro"))
obj <- newCoProSingle(
  normalizedData = toy$normalizedData,
  locationData   = toy$locationData,
  metaData       = toy$metaData,
  cellTypes      = toy$cellTypes
)
obj <- subsetData(obj, cellTypesOfInterest = unique(toy$cellTypes))
rng <- detectSigmaRange(obj, verbose = FALSE)
rng$sigmaValues
}
\seealso{
\code{\link[=computeKernelMatrix]{computeKernelMatrix()}}, \code{\link[=computeSparseKernelFloat32]{computeSparseKernelFloat32()}}

Other spatial-pipeline: 
\code{\link{computeDistance}()},
\code{\link{computeKernelMatrix}()},
\code{\link{computePCA}()},
\code{\link{computeSparseKernel}()},
\code{\link{computeSparseKernelFloat32}()},
\code{\link{runGeneSpaceCCA}()},
\code{\link{runSkrCCA}()},
\code{\link{runSlideLevelInference}()},
\code{\link{selectSigmaByPermutation}()}
}
\concept{spatial-pipeline}

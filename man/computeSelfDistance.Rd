% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_self_distance_kernel.R
\name{computeSelfDistance}
\alias{computeSelfDistance}
\alias{computeSelfDistance,CoProSingle-method}
\alias{computeSelfDistance,CoProMulti-method}
\title{Compute Self-Distance Matrices for Multiple Cell Types}
\usage{
computeSelfDistance(
  object,
  distType = c("Euclidean2D", "Euclidean3D", "Morphology-Aware"),
  xDistScale = 1,
  yDistScale = 1,
  zDistScale = 1,
  normalizeDistance = FALSE,
  normalizeMethod = c("spacing", "percentile"),
  truncateLowDist = TRUE,
  verbose = TRUE,
  overwrite = FALSE
)

\S4method{computeSelfDistance}{CoProSingle}(
  object,
  distType = c("Euclidean2D", "Euclidean3D", "Morphology-Aware"),
  xDistScale = 1,
  yDistScale = 1,
  zDistScale = 1,
  normalizeDistance = FALSE,
  normalizeMethod = c("spacing", "percentile"),
  truncateLowDist = TRUE,
  verbose = TRUE,
  overwrite = FALSE
)

\S4method{computeSelfDistance}{CoProMulti}(
  object,
  distType = c("Euclidean2D", "Euclidean3D", "Morphology-Aware"),
  xDistScale = 1,
  yDistScale = 1,
  zDistScale = 1,
  normalizeDistance = FALSE,
  normalizeMethod = c("spacing", "percentile"),
  truncateLowDist = TRUE,
  verbose = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{object}{A \code{CoPro} object with multiple cell types}

\item{distType}{Type of distance to compute: "Euclidean2D",
"Euclidean3D", or "Morphology-Aware"}

\item{xDistScale}{Scale for x distance, default = 1}

\item{yDistScale}{Scale for y distance, default = 1}

\item{zDistScale}{Scale for z distance, default = 1}

\item{normalizeDistance}{How to scale the self-distances. One of:
\describe{
\item{\code{FALSE} (default as of 1.2.0)}{No scaling; distances stay in the
coordinate units of \code{locationData}.}
\item{\code{TRUE}}{Derive a scaling factor from the self-distances' own
reference length (see \code{normalizeMethod}), so that reference becomes
0.01. This factor generally differs from the one \code{\link[=computeDistance]{computeDistance()}}
derived from the cross-type distances, which means a self-kernel at a
given nominal sigma sits at a different physical bandwidth from the
cross-kernel at the same sigma. A warning is issued when the two
disagree.}
\item{\code{"inherit"}}{Reuse the scaling factor \code{\link[=computeDistance]{computeDistance()}} recorded
on the object, so self- and cross-distances share one scale. This is
what you want whenever the self-kernels will be combined with the
cross-kernels -- notably as whitening operators in
\code{\link[=computeNormalizedCorrelation]{computeNormalizedCorrelation()}}. Errors if no factor has been
recorded.}
}}

\item{normalizeMethod}{How the reference distance is estimated when
\code{normalizeDistance = TRUE}. \code{"spacing"} (default) uses the median
nearest-partner distance, combined across cell-type blocks by median, so the
unit tracks local cell spacing and no single dense block sets the scale for
the whole object. \code{"percentile"} reproduces the pre-1.2.0 behavior: the
minimum, across blocks, of a low quantile of pairwise distances. Not
available for \code{distType = "Morphology-Aware"}, which falls back to
\code{"percentile"}. Ignored when \code{normalizeDistance} is \code{FALSE} or \code{"inherit"}.}

\item{truncateLowDist}{Whether to truncate small distances so that cells
that are nearly overlapping do not have super small distances. Default = TRUE.}

\item{verbose}{Whether to print info about the computation progress}

\item{overwrite}{Whether to overwrite existing distance matrices. If FALSE,
will add self-distance matrices to existing cross-type distances. Default = FALSE}
}
\value{
\code{CoPro} object with self-distance matrices added to the distances slot
}
\description{
This function computes within-cell-type distance matrices for each cell type
when multiple cell types are present. Unlike the standard computeDistance
which only computes cross-type distances for multiple cell types, this
function computes self-distances (within each cell type).
}
\examples{
\dontrun{
# Assume you have a CoPro object with multiple cell types
# First compute cross-type distances
object <- computeDistance(object)

# Then add self-distances
object <- computeSelfDistance(object)

# Now you have both cross-type and self-type distance matrices
}
}

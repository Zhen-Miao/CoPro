% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_constructors.R
\name{asCoPro}
\alias{asCoPro}
\alias{asCoProSingle}
\alias{asCoProMulti}
\alias{asCoProSingle,ANY-method}
\alias{asCoProMulti,ANY-method}
\title{Coerce a single-cell object into a CoPro object}
\usage{
asCoProSingle(x, spatialDim = "spatial", cellTypeCol,
              assay = NULL, layer = "data", ...)

asCoProMulti(x, spatialDim = "spatial", cellTypeCol, slideCol,
             assay = NULL, layer = "data", ...)
}
\arguments{
\item{x}{A \code{SingleCellExperiment} or \code{Seurat} object.}

\item{spatialDim}{Name of the reducedDim / image / reduction that holds
2-D (or 3-D) spatial coordinates. Default \code{"spatial"}.}

\item{cellTypeCol}{Name of the column in the object's cell-level metadata
that holds cell type labels. Required.}

\item{slideCol}{(\code{asCoProMulti()} only) Name of the column in the
object's cell-level metadata that holds the slide/sample identifier.}

\item{assay}{Optional assay name. For \code{SingleCellExperiment} defaults
to \code{"logcounts"} when available, otherwise the first assay. For
\code{Seurat} defaults to \code{SeuratObject::DefaultAssay(x)}.}

\item{layer}{Seurat-only. Layer (formerly "slot") to pull expression data
from. Default \code{"data"}.}

\item{...}{Additional arguments passed through to \code{\link[=newCoProSingle]{newCoProSingle()}} /
\code{\link[=newCoProMulti]{newCoProMulti()}}.}
}
\value{
A \code{CoProSingle} or \code{CoProMulti} object.
}
\description{
One-call coercion from \code{SingleCellExperiment} or \code{Seurat}
objects into \code{CoProSingle} (\code{asCoProSingle()}) or
\code{CoProMulti} (\code{asCoProMulti()}) objects. These helpers extract
the relevant expression matrix, spatial coordinates, cell metadata, and
cell-type labels, then delegate to \code{\link[=newCoProSingle]{newCoProSingle()}} /
\code{\link[=newCoProMulti]{newCoProMulti()}} so all validation stays single-sourced.
}
\details{
For \code{SingleCellExperiment} objects:
\itemize{
  \item \code{normalizedData} comes from \code{SummarizedExperiment::assay(x, assay)}
        (default \code{"logcounts"} if present, else the first assay),
        transposed into cells-by-genes orientation.
  \item \code{locationData} comes from
        \code{SingleCellExperiment::reducedDim(x, spatialDim)} (expected
        to have 2 or 3 columns renamed to \code{x}, \code{y}, optionally \code{z}).
  \item \code{metaData} comes from \code{SummarizedExperiment::colData(x)}
        as a data.frame.
  \item \code{cellTypes} comes from the column of \code{colData(x)} named
        \code{cellTypeCol}.
}

For \code{Seurat} objects:
\itemize{
  \item \code{normalizedData} comes from
        \code{SeuratObject::GetAssayData(x, layer = layer, assay = assay)},
        transposed into cells-by-genes orientation.
  \item \code{locationData} comes from either
        \code{x[[spatialDim]]@coordinates} (when \code{spatialDim} refers
        to a spatial image/slice) or
        \code{SeuratObject::Embeddings(x, reduction = spatialDim)} (when
        it refers to a dimensionality reduction).
  \item \code{metaData} comes from the object's \code{[[]]} slot.
  \item \code{cellTypes} comes from \code{x[[cellTypeCol]][, 1]}.
}

Both conversions require the corresponding package to be installed
(\code{SingleCellExperiment} / \code{SummarizedExperiment} or
\code{SeuratObject}) and will error with an install hint otherwise.
}
\seealso{
\code{\link[=newCoProSingle]{newCoProSingle()}}, \code{\link[=newCoProMulti]{newCoProMulti()}},
\code{\link[=CreateCoPro]{CreateCoPro()}}

Other object-creation:
\code{\link{newCoProSingle}()},
\code{\link{newCoProMulti}()},
\code{\link{CreateCoPro}()}
}
\concept{object-creation}

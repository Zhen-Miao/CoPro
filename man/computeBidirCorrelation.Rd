% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16_compute_bidirectional_corr.R
\name{computeBidirCorrelation}
\alias{computeBidirCorrelation}
\alias{computeBidirCorrelation,CoPro-method}
\alias{computeBidirCorrelation,CoProMulti-method}
\title{Compute Bidirectional Correlation}
\usage{
computeBidirCorrelation(
  object,
  calculationMode = "perSlide",
  normalize_K = c("row_or_col", "sinkhorn_knopp", "none"),
  filter_kernel = TRUE,
  K_row_sum_cutoff = 0.05,
  K_col_sum_cutoff = 0.05
)

\S4method{computeBidirCorrelation}{CoPro}(
  object,
  calculationMode = "perSlide",
  normalize_K = c("row_or_col", "sinkhorn_knopp", "none"),
  filter_kernel = TRUE,
  K_row_sum_cutoff = 0.05,
  K_col_sum_cutoff = 0.05
)

\S4method{computeBidirCorrelation}{CoProMulti}(
  object,
  calculationMode = "perSlide",
  normalize_K = c("row_or_col", "sinkhorn_knopp", "none"),
  filter_kernel = TRUE,
  K_row_sum_cutoff = 0.05,
  K_col_sum_cutoff = 0.05
)
}
\arguments{
\item{object}{A \code{CoPro} or \code{CoProMulti} object containing skrCCA results,
PCA results, and kernel matrices.}

\item{calculationMode}{(CoProMulti only) "perSlide" or "aggregate".
Ignored for single-slide objects.}

\item{normalize_K}{whether to normalize the kernel matrix, one of "none", "sinkhorn_knopp", "row_or_col"}

\item{filter_kernel}{whether to filter the kernel matrix, default is TRUE}

\item{K_row_sum_cutoff}{Numeric; cutoff for row sums when normalizing kernel matrix.
Default 5e-3.}

\item{K_col_sum_cutoff}{Numeric; cutoff for column sums when normalizing kernel matrix.
Default 5e-3.}
}
\value{
The input object with \verb{@bidirCorrelation} populated (a list keyed by
sigma names). For single-slide objects, each entry is a data frame with
columns \code{sigmaValues}, \code{cellType1}, \code{cellType2}, \code{CC_index}, \code{bidirCorrelation}.
For multi-slide objects, the columns depend on \code{calculationMode}.
}
\description{
Computes the mean of two correlations: cor(t(A_w1) \%\emph{\% K, B_w2) and
cor(A_w1, K \%}\% B_w2), where A_w1 and B_w2 are cell scores derived from
PCA matrices multiplied by skrCCA weights for the corresponding cell types,
and K is the kernel matrix between the two cell types.
}
\details{
The results are stored in the slot \code{bidirCorrelation}.

For multi-slide (\code{CoProMulti}) objects, results can be computed per slide
(default, returns a data frame with a \code{slideID} column) or aggregated across
slides (returns a data frame with an \code{aggregateCorrelation} column).
}
\examples{
# Assuming `obj` is a prepared CoProSingle with PCA, kernels, and skrCCA:
# obj <- computeBidirCorrelation(obj)

# For CoProMulti per-slide results:
# objm <- computeBidirCorrelation(objm, calculationMode = "perSlide")

# For CoProMulti aggregate results:
# objm <- computeBidirCorrelation(objm, calculationMode = "aggregate")
}

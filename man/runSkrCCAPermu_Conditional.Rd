% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/D_spatial_CCA_permutation.R
\name{runSkrCCAPermu_Conditional}
\alias{runSkrCCAPermu_Conditional}
\title{Conditional (sequential step-down) permutation test across canonical axes}
\usage{
runSkrCCAPermu_Conditional(
  object,
  nPermu = 100,
  sigma_values = NULL,
  permu_method = "bin",
  permu_which = "second_only",
  num_bins_x = NULL,
  num_bins_y = NULL,
  match_quantile = FALSE,
  alpha = 0.05,
  maxIter = 200,
  tol = 1e-05,
  verbose = TRUE
)
}
\arguments{
\item{object}{A CoPro object with \code{runSkrCCA()} and
\code{computeNormalizedCorrelation()} already run.}

\item{nPermu}{Number of permutations (default 100).}

\item{sigma_values}{Candidate bandwidths for the fair-sigma maximum. \code{NULL}
uses all \code{object@sigmaValues} that have kernels and weights.}

\item{permu_method}{Permutation null: "bin" (default), "global", "pc", or
"toroidal". See \code{\link[=runSkrCCAPermu]{runSkrCCAPermu()}}.}

\item{permu_which}{Which cell types to permute: "second_only" (default),
"both", or "first_only".}

\item{num_bins_x, num_bins_y}{Bin grid for \code{permu_method = "bin"}. Default
\code{NULL} is sigma-aware (see \code{\link[=.sigmaAwareBins]{.sigmaAwareBins()}}); the same grid is shared
across the sigma sweep. Pass integers to override.}

\item{match_quantile}{Whether to use quantile matching for bin permutation.}

\item{alpha}{Family-wise significance level for the step-down rule (default 0.05).}

\item{maxIter, tol}{Optimization controls passed to the axis optimizer.}

\item{verbose}{Whether to print progress and a summary (default TRUE).}
}
\value{
The CoPro object with results stored in the \verb{@conditionalPermu} slot,
a list whose \code{per_axis} element is a data frame of \code{CC_index},
\code{observed_stat}, \code{observed_sigma}, \code{p_raw}, \code{p_stepdown}, \code{mc_floor}, and
\code{significant}, plus the full null matrices for diagnostics. Use
\code{\link[=calculate_pvalue_stepdown]{calculate_pvalue_stepdown()}} to read the per-axis table.
}
\description{
Performs a permutation test that controls BOTH the sigma-selection
multiplicity (via a fair-sigma max-statistic, as in
\code{\link[=runSkrCCAPermu_FairSigma]{runSkrCCAPermu_FairSigma()}}) AND the canonical-axis multiplicity (via a
sequential conditional step-down test). This is the statistically correct
way to assign p-values to CC1, CC2, ... jointly, and it supersedes computing
a separate per-axis p-value from a full multi-component permutation, which is
anti-conservative for axes \code{k >= 2} when CC1 is strong.
}
\details{
\subsection{Why a conditional test}{

The observed CC2 is obtained by deflating the observed (real) CC1 direction
and optimizing the residual. A naive permutation p-value compares this to the
CC2 of fully re-optimized permutations, where each permutation deflates its
OWN leading axis. Those two statistics are produced by different operators
and are not exchangeable under the null, which biases the CC2 p-value
downward (too many false positives). The conditional test instead deflates
every permutation by the SAME fixed observed CC1..CC(k-1) directions, so the
observed and null axis-\code{k} statistics share one operator and are exchangeable.
}

\subsection{The statistic and step-down rule}{

For axis \code{k}, the statistic is the fair-sigma maximum over the candidate
bandwidths of the residual normalized correlation after deflating the fixed
observed CC1..CC(k-1) directions. The observed value is read from the stored
\code{normalizedCorrelation} (so it matches \code{\link[=getNormCorr]{getNormCorr()}} exactly); each
permutation re-optimizes the residual leading axis on permuted PCs. The raw
p-value uses the Phipson & Smyth (2010) estimator, which counts the observed
configuration as one admissible permutation so the p-value is never exactly
zero:

\deqn{p_{\mathrm{raw}}(k) = \frac{1 + \#\{\mathrm{perm}_k \ge \mathrm{obs}_k\}}{1 + m},}

with Monte-Carlo floor \code{1 / (m + 1)} (reported as \code{mc_floor}). Closed
step-down control of the family-wise error rate across ordered axes uses

\deqn{p_{\mathrm{stepdown}}(k) = \max_{j \le k} p_{\mathrm{raw}}(j),}

and testing stops at the first axis with \code{p_stepdown > alpha}; that axis and
all later ones are declared non-significant. No Bonferroni factor is needed.
This is the closed/fixed-sequence test of Marcus, Peritz & Gabriel (1976) and
the permutation "test of canonical axes" of Legendre, Oksanen & ter Braak
(2011).
}

\subsection{Relationship to data residualization}{

Deflating \code{Y} by the fixed observed weight directions is algebraically
identical to the Freedman-Lane / ter Braak residualization that removes the
observed lower canonical \emph{variates} from the data before recomputing the
cross-product, whenever the PCs are whitened (\code{scalePCs = TRUE}, so
\verb{X^T X = c I}): \verb{(I - u u^T) Y (I - v v^T) = Y - (u^T Y v)\\, u v^T}. The
fair-sigma maximum over the bandwidth family is the Westfall-Young (1993)
maxT procedure.
}
}
\examples{
\dontrun{
br <- runSkrCCA(br, scalePCs = TRUE, nCC = 3)
br <- computeNormalizedCorrelation(br)
br <- runSkrCCAPermu_Conditional(br, nPermu = 200, permu_method = "bin")
calculate_pvalue_stepdown(br)
}

}
\references{
Phipson B, Smyth GK (2010). Permutation P-values should never be zero.
\emph{Stat Appl Genet Mol Biol} 9:Article39.
Legendre P, Oksanen J, ter Braak CJF (2011). Testing the significance of
canonical axes in redundancy analysis. \emph{Methods Ecol Evol} 2:269-277.
Westfall PH, Young SS (1993). \emph{Resampling-based Multiple Testing}.
}
\seealso{
\code{\link[=runSkrCCAPermu_FairSigma]{runSkrCCAPermu_FairSigma()}} (the CC1 / sigma-multiplicity case),
\code{\link[=calculate_pvalue_stepdown]{calculate_pvalue_stepdown()}}
}

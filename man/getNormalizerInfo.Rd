% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15b_normalizer.R
\name{getNormalizerInfo}
\alias{getNormalizerInfo}
\title{Report the normalizer actually used}
\usage{
getNormalizerInfo(object)
}
\arguments{
\item{object}{A \code{CoPro} or \code{CoProMulti} object that has been through
\code{\link[=computeNormalizedCorrelation]{computeNormalizedCorrelation()}}.}
}
\value{
A list with the normalizer \code{mode}, a human-readable \code{description},
and the fitted autocorrelation \code{ranges} when \code{normalizer = "variogram"}
was used. \code{NULL} if the object predates this record.
}
\description{
The denominator of the normalized correlation determines how the criterion
behaves across the sigma grid, and it used to be decided implicitly by
whether the object happened to carry within-type kernels. This records the
resolved choice on the object so a stored result can be interpreted later.
}
\examples{
# obj <- computeNormalizedCorrelation(obj, normalizer = "variogram")
# getNormalizerInfo(obj)
}
\seealso{
\code{\link[=computeNormalizedCorrelation]{computeNormalizedCorrelation()}}

Other scores-and-correlation: 
\code{\link{computeGeneAndCellScores}()},
\code{\link{computeNormalizedCorrelation}()},
\code{\link{getCCAObjective}()}
}
\concept{scores-and-correlation}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/K_multi-slide.R
\name{computePCAMulti}
\alias{computePCAMulti}
\alias{computePCAMulti,CoProm-method}
\title{Compute PCA on Integrated Multi-Slide Data}
\usage{
computePCAMulti(
  object,
  nPCA = 40,
  center = TRUE,
  scale. = TRUE,
  dataUse = "raw",
  center_per_slide = FALSE,
  use_irlba = TRUE
)

\S4method{computePCAMulti}{CoProm}(
  object,
  nPCA = 40,
  center = TRUE,
  scale. = TRUE,
  dataUse = "raw",
  center_per_slide = FALSE,
  use_irlba = TRUE
)
}
\arguments{
\item{object}{A \code{CoProm} object with the \code{integratedData} slot populated.}

\item{nPCA}{Number of principal components to compute for each cell type.}

\item{center}{Whether to center the matrix before PCA}

\item{scale.}{Whether to scale the matrix before PCA}

\item{dataUse}{What data to use, choices between "raw" and "integrated".
Default is "raw"}

\item{center_per_slide}{After the global PCA, do we do center per slide
again? By default this is set to FALSE}

\item{use_irlba}{Logical, whether to use irlba for faster computation.}
}
\value{
A \code{CoProm} object with the \code{pcaResults} slot populated.
\code{pcaResults} structure: \code{list(slideID = list(cellType = pc_matrix))}.
}
\description{
Performs PCA on the integrated data stored within the \code{CoProm} object.
Assumes integration has created a common space across slides.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/C_resampling_function.R
\name{.sigmaAwareBins}
\alias{.sigmaAwareBins}
\title{Compute sigma-aware bin counts for spatial permutation}
\usage{
.sigmaAwareBins(object, sigma, min_bins = 2L, verbose = TRUE)
}
\arguments{
\item{object}{A CoPro object (uses \verb{@locationDataSub} and \verb{@distances}).}

\item{sigma}{Kernel bandwidth (numeric, on the normalized distance scale).}

\item{min_bins}{Minimum number of bins per axis (default 2).}

\item{verbose}{Whether to message the chosen grid (default TRUE).}
}
\value{
A list with integer \code{num_bins_x}, \code{num_bins_y}, and the recovered
\code{scale_factor}.
}
\description{
Internal helper that chooses the number of bins for bin-wise spatial
permutation from the kernel bandwidth \code{sigma} instead of a fixed hard-coded
grid. The target patch side is \code{2 * sigma} on the normalized distance scale,
so each patch is large enough to preserve within-type spatial
autocorrelation (length scale \code{~sigma}) while shuffling whole patches still
breaks longer-range cross-type coordination. A hard-coded grid (e.g. the
historical 10x10) ignores both the tissue extent and \code{sigma}, which is the
root cause of mis-calibrated bin-wise permutation.
}
\keyword{internal}

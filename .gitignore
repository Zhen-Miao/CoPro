.Rproj.user
docs
.Rhistory
.DS_Store
.upload
CLAUDE.md
data-raw/vignette_data/
README.html
README.knit.md
*.tar.gz
*.Rcheck/
experiments/
reports/skrcca_svd_evaluation/
src/*.o
src/*.so

# Coding-agent session state and its instruction file, kept alongside the
# already-ignored CLAUDE.md so a fresh clone carries neither.
.claude/
.workbuddy/
AGENTS.md

# Working-directory / rendered artifacts (not part of the package)
Rplots.pdf
embedding.tsv
vignettes/*.html
reports/perf_pass_baseline/current/

# Benchmark output folders: scripts and small CSVs are worth keeping, but these
# also hold multi-hundred-MB .rds objects that must not reach the remote.
reports/skrcca_y_precompute_speed/
tests/testthat/testthat-problems.rds
tests/testthat/_problems/

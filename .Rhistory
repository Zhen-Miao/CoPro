setwd("/Users/zhenmiao/Documents/GitHub/CoPro/")
devtools::load_all()
setwd("/Users/zhenmiao/Dropbox/DIALOGUE_plus project/Data/kidney_seqFISH/")
kid <- readRDS("kidney_IRI2_with_xy.rds") ## IRI 2 sample only
library("CoPro")
library_paths <- c(
"fields", "ggplot2", "tictoc", "irlba",
"Matrix", "ggsci", "ape"
)
# Load required libraries
lapply(library_paths, library, character.only = TRUE)
n_pca <- 40 ## number of top PCs to retain
cell_types <- c("all_PT", "Fib")
n_cell_types <- length(cell_types)
horizontal_dist_scale <- 1
vertical_dist_scale <- 1
## get the subset from the data
meta <- kid@meta.data
ra <- t(kid@assays$Spatial@data)
meta$Celltype <- as.character(meta$Celltype)
meta$Celltype[meta$Celltype %in% c("Injured PT", "PTS1", "PTS2", "PTS3")] = "all_PT"
subset_cells <- meta$Celltype %in% cell_types
ra <- ra[subset_cells, ]
meta <- meta[subset_cells, ]
## make sure coordinates are numeric
meta$x <- as.numeric(meta$x_um) / 1000
meta$y <- as.numeric(meta$y_um) / 1000
## show
tb <- table(meta$ME)
meta_ME <- meta[meta$ME %in% names(tb[tb>500]), ]
meta_ME$ME_5orother <- as.character(meta_ME$ME)
meta_ME$ME_5orother[meta_ME$ME_5orother != "5"] = "other"
location_data <- meta[, c("x", "y")]
cell_type_data <- meta[, "Celltype"]
br <- newCoPro(
normalizedData = ra,
locationData = location_data,
metaData = meta,
cellTypes = cell_type_data
)
br <- subsetData(br, cellTypesOfInterest = cell_types)
br <- computePCA(br, nPCA = n_pca, center = TRUE, scale. = TRUE)
sigma_choice <- sqrt(c(0.01, 0.02, 0.05, 0.1, 0.2))
# sigma_square_choice <- c(0.01, 0.02, 0.05)
## By default, we normalize the distance
br <- computeDistance(br, distType = "Euclidean2D", normalizeDistance = FALSE)
## compute kernel
br <- computeKernelMatrix(br, sigmaValues = sigma_choice,
normalizeKernel = TRUE, lowerLimit = 5e-7)
br <- runSkrCCA(br, scalePCs = TRUE, maxIter = 500, nCC = 4)
### run CoPro on kidney data
library(Seurat)
library(ggplot2)
setwd("/Users/zhenmiao/Documents/GitHub/CoPro/")
devtools::load_all()
setwd("/Users/zhenmiao/Dropbox/DIALOGUE_plus project/Data/kidney_seqFISH/")
kid <- readRDS("kidney_IRI2_with_xy.rds") ## IRI 2 sample only
library("CoPro")
library_paths <- c(
"fields", "ggplot2", "tictoc", "irlba",
"Matrix", "ggsci", "ape"
)
# Load required libraries
lapply(library_paths, library, character.only = TRUE)
n_pca <- 40 ## number of top PCs to retain
cell_types <- c("all_PT", "Fib")
n_cell_types <- length(cell_types)
horizontal_dist_scale <- 1
vertical_dist_scale <- 1
## get the subset from the data
meta <- kid@meta.data
ra <- t(kid@assays$Spatial@data)
meta$Celltype <- as.character(meta$Celltype)
meta$Celltype[meta$Celltype %in% c("Injured PT", "PTS1", "PTS2", "PTS3")] = "all_PT"
subset_cells <- meta$Celltype %in% cell_types
ra <- ra[subset_cells, ]
meta <- meta[subset_cells, ]
## make sure coordinates are numeric
meta$x <- as.numeric(meta$x_um) / 1000
meta$y <- as.numeric(meta$y_um) / 1000
## show
tb <- table(meta$ME)
meta_ME <- meta[meta$ME %in% names(tb[tb>500]), ]
meta_ME$ME_5orother <- as.character(meta_ME$ME)
meta_ME$ME_5orother[meta_ME$ME_5orother != "5"] = "other"
location_data <- meta[, c("x", "y")]
cell_type_data <- meta[, "Celltype"]
br <- newCoPro(
normalizedData = ra,
locationData = location_data,
metaData = meta,
cellTypes = cell_type_data
)
br <- subsetData(br, cellTypesOfInterest = cell_types)
br <- computePCA(br, nPCA = n_pca, center = TRUE, scale. = TRUE)
sigma_choice <- sqrt(c(0.01, 0.02, 0.05, 0.1, 0.2))
# sigma_square_choice <- c(0.01, 0.02, 0.05)
## By default, we normalize the distance
br <- computeDistance(br, distType = "Euclidean2D", normalizeDistance = FALSE)
## compute kernel
br <- computeKernelMatrix(br, sigmaValues = sigma_choice,
# upperQuantile = 0.90,
normalizeKernel = TRUE, lowerLimit = 5e-7)
br <- runSkrCCA(br, scalePCs = TRUE, maxIter = 500, nCC = 4)
br <- computeNormalizedCorrelation(br)
br <- computeGeneAndCellScores(br)
setwd("/Users/zhenmiao/Documents/GitHub/CoPro/")
devtools::load_all()
library("CoPro")
br <- computeGeneAndCellScores(br)
setwd("/Users/zhenmiao/Documents/GitHub/CoPro/")
devtools::load_all()
br <- computeGeneAndCellScores(br)
#' @rdname computeGeneAndCellScores
#' @aliases computeGeneAndCellScores,CoPro-method
#' @export
setMethod(
"computeGeneAndCellScores", "CoPro",
function(object) {
## Check for required components
if (length(object@skrCCAOut) == 0) {
stop("CCA results are not available. Please run CCA first.")
}
if (length(object@kernelMatrices) == 0) {
stop(paste("Kernel matrices are not available.",
"Please compute the kernel matrices first."))
}
## choose cell types
if (length(object@cellTypesOfInterest) != 0) {
cts <- object@cellTypesOfInterest
} else {
warning(paste(
"no cell type of interest specified,",
"using all cell types to run the analysis"
))
cts <- unique(object@cellTypesSub)
}
## check sigmaValues
if (length(object@sigmaValues) == 0) {
stop("sigmaValues is empty, please specify")
}
sigmaValues <- object@sigmaValues
nCC <- object@nCC
## load whether the PCs are being scaled prior to CCA
if (length(object@scalePCs) == 0) {
stop("object@scalePCs not specified")
}
scalePCs <- object@scalePCs
# allPCs = object@pcaResults
PCmats <- .getAllPCs(allPCs = object@pcaResults, scalePCs = scalePCs)
sigma_names <- paste("sigma", sigmaValues, sep = "_")
## cell scores and gene scores are both by cell types
cellScores <- setNames(
vector(mode = "list", length = length(sigmaValues)),
sigma_names
)
for (t in sigma_names) {
cellScores[[t]] <- setNames(
vector(mode = "list", length = length(cts)),
cts
)
}
for (t in sigma_names) {
for(i in cts){
cellScores[[t]][[i]] <- matrix(
nrow = sum(object@cellTypesSub == i),
ncol = nCC
)
colnames(cellScores[[t]][[i]]) <- paste0("CC_", 1:nCC)
rownames(cellScores[[t]][[i]]) <- rownames(object@normalizedDataSub)[
object@cellTypesSub == i
]
}
}
## gene scores
geneScores <- setNames(
vector(mode = "list", length = length(sigmaValues)),
sigma_names
)
for (t in sigma_names) {
geneScores[[t]] <- setNames(
vector(mode = "list", length = length(cts)),
cts
)
}
for (t in sigma_names) {
for (i in cts) {
geneScores[[t]][[i]] <- matrix(
nrow = ncol(object@normalizedDataSub),
ncol = nCC
)
colnames(geneScores[[t]][[i]]) <- paste0("CC_", 1:nCC)
rownames(geneScores[[t]][[i]]) <- colnames(object@normalizedDataSub)
}
}
## go over all cell types, then over all sigma values
for (tt in seq_along(sigmaValues)) {
t <- sigma_names[tt]
for (i in cts) {
for(cc_index in seq_len(nCC)){
cc_name <- paste0("CC_", cc_index)
w_1 <- object@skrCCAOut[[t]][[i]][,cc_index, drop = FALSE]
if (scalePCs) {
geneScores[[t]][[i]][, cc_name] <- as.vector(
matrix(w_1 * object@pcaResults[[i]]$sdev, nrow = 1) %*%
t(object@pcaResults[[i]]$rotation)
)
} else {
geneScores[[t]][[i]][, cc_name] <- as.vector(
matrix(w_1, nrow = 1) %*%
t(object@pcaResults[[i]]$rotation)
)
}
cellScores[[t]][[i]][, cc_name] <- as.vector(PCmats[[i]] %*% w_1)
}
}
}
## save cellscores and gene scores
object@cellScores <- cellScores
object@geneScores <- geneScores
## add cell score information to the cell metadata
meta_t <- stats::setNames(
vector(mode = "list", length = length(cts)),
cts
)
for (i in cts) {
meta_t[[i]] <- object@metaDataSub[object@cellTypesSub == i, ]
for (t in sigma_names){
for(cc_index in seq_len(nCC)){
cc_name <- paste0("CC_", cc_index)
cellScoreColName <- paste0("cellScore_", t, "_cc_index_", cc_index)
meta_t[[i]][,cellScoreColName] <-
cellScores[[t]][[i]][rownames(meta_t[[i]]),cc_name]
}
}
}
## combine each cell type meta.data back
names(meta_t) <- NULL
meta_all <- do.call(rbind, meta_t)[rownames(object@metaDataSub), ]
object@metaDataSub <- meta_all
return(object)
}
)
br <- computeGeneAndCellScores(br)
ncorr <- getNormCorr(br)
ggplot(data = ncorr, aes(
x = sigmaValues,
y = normalizedCorrelation, group = 1
)) +
geom_point() +
geom_line() +
facet_wrap(facets = ~ ct12 + CC_index,
labeller = purrr::partial(label_both, sep = " = ")) +
xlab("Sigma") +
ylab("Norm. Corr.") +
ggtitle(label = "Norm. Corr. across sigma values") +
theme_minimal()
ncorr
br <- computeNormalizedCorrelationApprox(br)
ncorr <- getNormCorr(br)
ncorr
?irlba
br <- computeNormalizedCorrelation(br, tol = 1e-3)
setwd("/Users/zhenmiao/Documents/GitHub/CoPro/")
devtools::load_all()
br <- computeNormalizedCorrelation(br, tol = 1e-3)
ncorr <- getNormCorr(br)
ncorr
devtools::check()
lintr::lint_package()
usethis::use_version()
renv::snapshot()
### run CoPro on kidney data
library(Seurat)
library(ggplot2)
setwd("/Users/zhenmiao/Documents/GitHub/CoPro/")
devtools::load_all()
setwd("/Users/zhenmiao/Dropbox/DIALOGUE_plus project/Data/kidney_seqFISH/")
kid <- readRDS("kidney_IRI2_with_xy.rds") ## IRI 2 sample only
library("CoPro")
library_paths <- c(
"fields", "ggplot2", "tictoc", "irlba",
"Matrix", "ggsci", "ape"
)
# Load required libraries
lapply(library_paths, library, character.only = TRUE)
n_pca <- 40 ## number of top PCs to retain
cell_types <- c("all_PT", "Fib")
n_cell_types <- length(cell_types)
horizontal_dist_scale <- 1
vertical_dist_scale <- 1
## get the subset from the data
meta <- kid@meta.data
ra <- t(kid@assays$Spatial@data)
meta$Celltype <- as.character(meta$Celltype)
meta$Celltype[meta$Celltype %in% c("Injured PT", "PTS1", "PTS2", "PTS3")] = "all_PT"
subset_cells <- meta$Celltype %in% cell_types
ra <- ra[subset_cells, ]
meta <- meta[subset_cells, ]
## make sure coordinates are numeric
meta$x <- as.numeric(meta$x_um) / 1000
meta$y <- as.numeric(meta$y_um) / 1000
## show
tb <- table(meta$ME)
meta_ME <- meta[meta$ME %in% names(tb[tb>500]), ]
meta_ME$ME_5orother <- as.character(meta_ME$ME)
meta_ME$ME_5orother[meta_ME$ME_5orother != "5"] = "other"
location_data <- meta[, c("x", "y")]
cell_type_data <- meta[, "Celltype"]
br <- newCoPro(
normalizedData = ra,
locationData = location_data,
metaData = meta,
cellTypes = cell_type_data
)
br <- subsetData(br, cellTypesOfInterest = cell_types)
br <- computePCA(br, nPCA = n_pca, center = TRUE, scale. = TRUE)
sigma_choice <- sqrt(c(0.01, 0.02, 0.05, 0.1, 0.2))
# sigma_square_choice <- c(0.01, 0.02, 0.05)
## By default, we normalize the distance
br <- computeDistance(br, distType = "Euclidean2D", normalizeDistance = FALSE)
## compute kernel
br <- computeKernelMatrix(br, sigmaValues = sigma_choice,
upperQuantile = 0.90,
normalizeKernel = TRUE, lowerLimit = 5e-7)
br <- runSkrCCA(br, scalePCs = TRUE, maxIter = 500, nCC = 4)
br <- computeNormalizedCorrelation(br, tol = 1e-3)
# br <- computeNormalizedCorrelationApprox(br)
br <- computeGeneAndCellScores(br)
ncorr <- getNormCorr(br)
ggplot(data = ncorr, aes(
x = sigmaValues,
y = normalizedCorrelation, group = 1
)) +
geom_point() +
geom_line() +
facet_wrap(facets = ~ ct12 + CC_index,
labeller = purrr::partial(label_both, sep = " = ")) +
xlab("Sigma") +
ylab("Norm. Corr.") +
ggtitle(label = "Norm. Corr. across sigma values") +
theme_minimal()
ncorr
rm(list = ls())
### run CoPro on kidney data
library(Seurat)
library(ggplot2)
setwd("/Users/zhenmiao/Documents/GitHub/CoPro/")
devtools::load_all()
setwd("/Users/zhenmiao/Dropbox/DIALOGUE_plus project/Data/kidney_seqFISH/")
kid <- readRDS("kidney_IRI2_with_xy.rds") ## IRI 2 sample only
library("CoPro")
library_paths <- c(
"fields", "ggplot2", "tictoc", "irlba",
"Matrix", "ggsci", "ape"
)
# Load required libraries
lapply(library_paths, library, character.only = TRUE)
n_pca <- 40 ## number of top PCs to retain
cell_types <- c("all_PT", "Fib")
n_cell_types <- length(cell_types)
horizontal_dist_scale <- 1
vertical_dist_scale <- 1
## get the subset from the data
meta <- kid@meta.data
ra <- t(kid@assays$Spatial@data)
meta$Celltype <- as.character(meta$Celltype)
meta$Celltype[meta$Celltype %in% c("Injured PT", "PTS1", "PTS2", "PTS3")] = "all_PT"
subset_cells <- meta$Celltype %in% cell_types
ra <- ra[subset_cells, ]
meta <- meta[subset_cells, ]
## make sure coordinates are numeric
meta$x <- as.numeric(meta$x_um) / 1000
meta$y <- as.numeric(meta$y_um) / 1000
## show
tb <- table(meta$ME)
meta_ME <- meta[meta$ME %in% names(tb[tb>500]), ]
meta_ME$ME_5orother <- as.character(meta_ME$ME)
meta_ME$ME_5orother[meta_ME$ME_5orother != "5"] = "other"
location_data <- meta[, c("x", "y")]
cell_type_data <- meta[, "Celltype"]
br <- newCoPro(
normalizedData = ra,
locationData = location_data,
metaData = meta,
cellTypes = cell_type_data
)
rm(kid)
br <- subsetData(br, cellTypesOfInterest = cell_types)
br <- computePCA(br, nPCA = n_pca, center = TRUE, scale. = TRUE)
sigma_choice <- sqrt(c(0.001, 0.002,0.005, 0.008 ,0.01, 0.02, 0.05))
# sigma_choice <- sqrt(c(0.001, 0.002,0.005, 0.008 ,0.01, 0.02, 0.05, 0.1, 0.2))
# sigma_square_choice <- c(0.01, 0.02, 0.05)
## By default, we normalize the distance
br <- computeDistance(br, distType = "Euclidean2D", normalizeDistance = FALSE)
## compute kernel
br <- computeKernelMatrix(br, sigmaValues = sigma_choice,
upperQuantile = 0.90,
normalizeKernel = TRUE, lowerLimit = 5e-7)
br <- runSkrCCA(br, scalePCs = TRUE, maxIter = 500, nCC = 4)
br <- computeNormalizedCorrelation(br, tol = 1e-3)
# br <- computeNormalizedCorrelationApprox(br)
br <- computeGeneAndCellScores(br)
ncorr <- getNormCorr(br)
ggplot(data = ncorr, aes(
x = sigmaValues,
y = normalizedCorrelation, group = 1
)) +
geom_point() +
geom_line() +
facet_wrap(facets = ~ ct12 + CC_index,
labeller = purrr::partial(label_both, sep = " = ")) +
xlab("Sigma") +
ylab("Norm. Corr.") +
ggtitle(label = "Norm. Corr. across sigma values") +
theme_minimal()
ggsave("Norm corr vs sigma four CC all_PT vs Fib new sigma set.pdf", width = 7, height = 7)
saveRDS(br, "kidney IRI_2 Fib vs all_PT sigma new sigma set.rds")
ncorr
# br <- computeNormalizedCorrelationApprox(br)
br <- computeGeneAndCellScores(br,sigma_choice = 0.1)
ncorr$sigmaSquared <- ncorr$sigmaValues^2
ncorr$sigmaSquared <- as.numeric(ncorr$sigmaValues)^2
ggplot(data = ncorr, aes(
x = sigmaValues,
y = normalizedCorrelation, group = 1
)) +
geom_point() +
geom_line() +
facet_wrap(facets = ~ ct12 + CC_index,
labeller = purrr::partial(label_both, sep = " = ")) +
xlab("Sigma") +
ylab("Norm. Corr.") +
ggtitle(label = "Norm. Corr. across sigma values") +
theme_minimal()
ggplot(data = ncorr, aes(
x = sigmaSquared,
y = normalizedCorrelation, group = 1
)) +
geom_point() +
geom_line() +
facet_wrap(facets = ~ ct12 + CC_index,
labeller = purrr::partial(label_both, sep = " = ")) +
xlab("Sigma") +
ylab("Norm. Corr.") +
ggtitle(label = "Norm. Corr. across sigma values") +
theme_minimal()
ncorr <- getNormCorr(br)
ncorr$sigmaSquared <- as.factor(as.numeric(ncorr$sigmaValues)^2)
ggplot(data = ncorr, aes(
x = sigmaSquared,
y = normalizedCorrelation, group = 1
)) +
geom_point() +
geom_line() +
facet_wrap(facets = ~ ct12 + CC_index,
labeller = purrr::partial(label_both, sep = " = ")) +
xlab("sigmaSquared") +
ylab("Norm. Corr.") +
ggtitle(label = "Norm. Corr. across sigma values") +
theme_minimal()
ncorr$sigmaSquared
head(ncorr$sigmaValues)
head(as.numeric(ncorr$sigmaValues))
ncorr <- getNormCorr(br)
ncorr$sigmaSquared <- as.character(as.numeric(as.character(ncorr$sigmaValues))^2)
ggplot(data = ncorr, aes(
x = sigmaSquared,
y = normalizedCorrelation, group = 1
)) +
geom_point() +
geom_line() +
facet_wrap(facets = ~ ct12 + CC_index,
labeller = purrr::partial(label_both, sep = " = ")) +
xlab("sigmaSquared") +
ylab("Norm. Corr.") +
ggtitle(label = "Norm. Corr. across sigma values") +
theme_minimal()
ncorr$sigmaSquared
ncorr$sigmaSquared <- as.character(signif(as.numeric(as.character(ncorr$sigmaValues))^2), 3)
ncorr$sigmaSquared
ggplot(data = ncorr, aes(
x = sigmaSquared,
y = normalizedCorrelation, group = 1
)) +
geom_point() +
geom_line() +
facet_wrap(facets = ~ ct12 + CC_index,
labeller = purrr::partial(label_both, sep = " = ")) +
xlab("sigmaSquared") +
ylab("Norm. Corr.") +
ggtitle(label = "Norm. Corr. across sigma values") +
theme_minimal()
ggsave("Norm corr vs sigma four CC all_PT vs Fib new sigma set.pdf", width = 7, height = 7)
?sample
?replicate
num_permutations <- 100
index_set_size <- 10  # Change this to match your specific index set size
# Use replicate to generate 100 permutations and save them in a matrix
permuted_matrix <- t(replicate(num_permutations, sample.int(index_set_size, replace = FALSE)))
dim(permuted_matrix)
??irlba
x  <- matrix(rnorm(200), nrow=20)
p1 <- prcomp_irlba(x, n=3)
library(irlba)
x  <- matrix(rnorm(200), nrow=20)
p1 <- prcomp_irlba(x, n=3)
summary(p1)
dim(p1$x)
devtools::check()
devtools::check()
devtools::check()
